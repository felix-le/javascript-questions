// call back functions are used to handle async operations in js
// the example : setTimeout() is a call back function

document.getElementById("btn").addEventListener("click", function () {
  console.log("Button clicked");
});
// the above code is an example of a call back function
// the function is called when the button is clicked
// the function is passed as an argument to the addEventListener method
